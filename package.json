{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "babel-eslint": "^10.0.1",
    "lerna": "^3.4.3",
    "standard": "^11.0.1"
  },
  "scripts": {
    "babel": "lerna exec --parallel --ignore '@citation-js/cli' -- babel src -d lib --copy-files --root $PWD",
    "lint": "standard"
  },
  "standard": {
    "parser": "babel-eslint",
    "globals": [
      "logger"
    ]
  }
}
